import Quick
import Nimble
@testable import PodStaticFrameworkExample

class ExampleSpec: QuickSpec {
  override func spec() {
    describe("something") {
      it("should do the right thing") {
        expect(true) == true
      }
    }
  }
}
